language: php

php:
  - 5.3
  - 5.4

before_script:
    - php -r "echo ini_get('include_path');"
    - ls -alh "/home/vagrant/.phpenv/versions/5.4.0/share/pyrus/.pear/php"
#    - ls /usr/share/pear/
#    - export TEST_HOME=$PWD
#    - cd / && patch -p1 < "$TEST_HOME/Tests/phpunit.patch"
#    - cd $TEST_HOME
#    - curl -s http://getcomposer.org/installer | php
#    - php composer.phar --dev install
